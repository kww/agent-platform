{
  "name": "agent-platform",
  "private": true,
  "version": "0.0.1",
  "deprecated": "This package is deprecated. Use @dommaker/harness, @dommaker/runtime, @dommaker/workflows, and agent-studio instead.",
  "description": "[DEPRECATED] Agent Platform Monorepo — use agent-studio instead",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter @dommaker/studio-api dev",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}